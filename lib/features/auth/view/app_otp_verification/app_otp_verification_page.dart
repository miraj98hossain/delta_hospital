import 'package:flutter/material.dart';

import '../../app_otp_verification.dart';

class AppOtpVerificationPage extends StatelessWidget {
  const AppOtpVerificationPage({super.key});
  static const routeName = 'app-otp-verification-page';
  static const routePath = '/app-otp-verification-page';
  @override
  Widget build(BuildContext context) {
    return const AppOtpVerificationView();
  }
}
