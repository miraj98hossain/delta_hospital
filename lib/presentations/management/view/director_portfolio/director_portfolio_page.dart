import 'package:flutter/material.dart';

import '../../director_portfolio.dart';

class DirectorPortfolioPage extends StatelessWidget {
  const DirectorPortfolioPage({super.key});
  static const String routeName = "director-portfolio-page";
  static const String routePath = "director-portfolio-page";
  @override
  Widget build(BuildContext context) {
    return const DirectorportfolioView();
  }
}
